export type ShippingAddress = {
  firstName: string;
  lastName: string;
  streetAddress: string;
  complStreetAddress?: string;
  city: string;
  postalCode: string;
  country: string;
  phoneNumber: string;
};
